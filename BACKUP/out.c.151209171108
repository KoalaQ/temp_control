#include<iom128v.h>
#include "out.h"
#include "lcd.h"
uint hz=0;
uint hz2=0;

//0、1的传入。port由0~7
void out_port(void){
  
   if(hz==0){
	 //hz=100;
	 PORTE=~PORTE;
	// lcd_write_char(0x03,3,0x30 | hz2++);
	// if(hz2==100){
	//  hz2=0;
	// }	
   }
    //hz--;
}
uint read_port(uint port){
  return PINC&(1<<port);
}